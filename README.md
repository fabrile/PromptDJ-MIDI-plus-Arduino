# this repository is from AI Studio app, it's not ready yet, still working
# due to posible Gemini changes, it may not work in the future.

This contains everything you need to run your app locally.

View the app in AI Studio: https://ai.studio/apps/drive/1htPh2-dBJNIV7xJQY1z5E1m533vXaiOz

## Run Locally

**Prerequisites:**  Node.js


1. Install dependencies:
   `npm install`
2. Set the `GEMINI_API_KEY` in [.env.local](.env.local) to your Gemini API key
3. Run the app:
   `npm run dev`
